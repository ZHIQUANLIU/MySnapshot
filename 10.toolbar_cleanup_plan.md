# Toolbar Neatness & Clipboard Implementation Plan

Goal: Transform the current messy toolbar into a professional, neatly aligned interface and add a "Copy to Clipboard" feature.

## User Review Required

> [!IMPORTANT]
> - **Unified Button Sizing**: I will enforce a standard minimum width for all tool buttons (Rectangle, Highlight, etc.) so they appear as a uniform set.
> - **Copy to Clipboard**: A new button will allow you to instantly copy the current annotated image to your clipboard for pasting into other apps.

## Proposed Changes

### [MODIFY] [browser_window.py](file:///c:/OpenCode/MySnapshot/browser_window.py)
 - **Toolbar Uniformity**:
    - Assign a consistent `setMinimumWidth` (e.g., 90-110px depending on language) to the tool buttons.
    - Use a more structured layout with grouped buttons inside sub-layouts or frames for better alignment.
    - Adjust `ResponsiveButton` to handle uniform width constraints while still scaling the font.
 - **New Functionality**:
    - Add `copy_to_clipboard_btn` to the action section.
    - Implement `copy_to_clipboard()`: Renders the `AnnotationScene` into a `QPixmap` and puts it in `QApplication.clipboard()`.

### [MODIFY] [styles.py](file:///c:/OpenCode/MySnapshot/styles.py)
 - **Border & Spacing**: Refine the button borders and margins to ensure the unified width doesn't look cramped.

## Verification Plan

### Manual Verification
 - **Neatness Check**: Resize the window and verify that the "Tools" row (Rect, High, etc.) maintains a uniform, rhythmic appearance.
 - **Clipboard Test**: Select an image, draw an arrow, click "Copy", and try pasting (`Ctrl+V`) into a chat or document.
